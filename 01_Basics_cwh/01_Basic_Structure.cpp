// This is how to write a comment.

# include <iostream>
using namespace std; //This means that in std, those functions which exists in std don't need to be called by another method,
// but just normally. (probably like iostream.cout => cout)
// This is where u import something imilar to modules in C++

// This is must syntax to start with int main
int main(){
    cout<<"Hey"; // cout is a function that does the functions of printing.
    // You can also use endl to print on new line.
    cout<<"Hello there"<<endl;
    cout<<"Hello there"<<endl;
    // Below, 'return' part is a must too.
    // This 'return 0;' means succesfull termination of program.
    return 0; // Mandatory statement.
}